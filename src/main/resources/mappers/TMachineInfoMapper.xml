<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lcyzh.nmerp.dao.TMachineInfoMapper">

    <resultMap id="tMachineInfoResultMap" type="com.lcyzh.nmerp.entity.TMachineInfo">
        <id column="id" property="id" jdbcType="BIGINT"/> 
        <result column="mac_code" property="macCode" jdbcType="VARCHAR"/> 
        <result column="prod_codes" property="prodCodes" jdbcType="VARCHAR"/> 
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/> 
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/> 

        
        
    </resultMap>


    <select id="get" resultMap="tMachineInfoResultMap">
        SELECT *
        FROM T_MACHINE_INFO
        WHERE id = #{id}
    </select>

    <select id="findList" resultMap="tMachineInfoResultMap">
        SELECT *
        FROM T_MACHINE_INFO
        <where>
        </where>
    </select>

    <select id="findAllList" resultMap="tMachineInfoResultMap">
        SELECT *
        FROM T_MACHINE_INFO
    </select>

    <insert id="insert" parameterType ="com.lcyzh.nmerp.entity.TMachineInfo">
        INSERT INTO T_MACHINE_INFO(
            id,
            mac_code,
            prod_codes,
            create_time,
            update_time
        )
        VALUES (
            #{id,jdbcType=BIGINT},
            #{macCode,jdbcType=VARCHAR},
            #{prodCodes,jdbcType=VARCHAR},
            #{createTime,jdbcType=TIMESTAMP},
            #{updateTime,jdbcType=TIMESTAMP}
        )
    </insert>

    <insert id="insertBatch" parameterType="com.lcyzh.nmerp.entity.TMachineInfo">
        INSERT INTO T_MACHINE_INFO(
            id,
            mac_code,
            prod_codes,
            create_time,
            update_time
        )
        VALUES
        <foreach collection ="list" item="tMachineInfo" separator =",">
        (
            #{tMachineInfo.id,jdbcType=BIGINT},
            #{tMachineInfo.macCode,jdbcType=VARCHAR},
            #{tMachineInfo.prodCodes,jdbcType=VARCHAR},
            #{tMachineInfo.createTime,jdbcType=TIMESTAMP},
            #{tMachineInfo.updateTime,jdbcType=TIMESTAMP}
        )
        </foreach>
    </insert>

    <update id="update"  parameterType ="com.lcyzh.nmerp.entity.TMachineInfo">
        UPDATE T_MACHINE_INFO
        SET
        id = #{id},
        mac_code = #{macCode},
        prod_codes = #{prodCodes},
        create_time = #{createTime},
        update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="delete"  parameterType ="com.lcyzh.nmerp.entity.TMachineInfo">
        DELETE FROM T_MACHINE_INFO
        WHERE id = #{id}
    </delete>

</mapper>